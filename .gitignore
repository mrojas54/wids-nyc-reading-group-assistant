# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp

# Local config / credentials
.env
.env.local
.envrc
*.pem
*.key

# Build artifacts
__pycache__/
*.pyc
node_modules/

# Python tooling (uv virtualenv + tool caches; pyproject.toml & uv.lock ARE tracked)
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
dist/
build/
.vercel
.env*.local

# ONNX model artifacts (large binary, stored in Vercel Blob)
specter2_int8.onnx

# Claude agent worktrees
.claude/worktrees/

# Agent tool scratch (per-developer, not part of the repo)
# claude-mem auto-injects empty CLAUDE.md context markers on directory reads
CLAUDE.md
# Firecrawl CLI / MCP scratch dir
.firecrawl/
# Serena MCP scratch dir
.serena/
# `remember` skill session-handoff scratch dir
.remember/
